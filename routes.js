// POST /data – create a new record in the databas
const postData = (req,res)=>{

}

// GET /data – read all the records for a MAC address
const getData = (req,req)=>{

}
// GET /data/:id – read one record
const readRecord = (req,res)=>{

}

// DELETE /data/:id – delete one record
const delData = (req, res)=>{

}

module.exports = {
    postData,
    getData,
    readRecord,
    delData
}
